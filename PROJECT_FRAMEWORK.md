# 《静默星图入口系统》项目框架与交互说明

> 文档基于 2026-09-06 的当前网页版本整理，用于产品交接、后续开发和 Hackathon 展示准备。
>
> 本文刻意忽略 B–06「共同创造生物」的视觉、生成方式和交互实现。它只作为星图中的预留节点出现，不纳入当前系统说明。

## 1. 项目概述

《静默星图入口系统》是一套以“年龄、权限与责任”为核心的互动网页。用户从一张公共服务星图进入不同场所，参与游戏、交换、消费和观察；与此同时，系统每隔一段时间要求用户确认制度年龄。年龄增长不仅改变数字，还会逐步开放管理权限，使用户从普通参与者变成能够干预他人可见性、准入、劳动、记忆和规则的管理者。

作品的核心并不是完成任务或获得高分，而是让用户面对一个持续变化的问题：当一个人拥有越来越多权限时，他如何使用这些权限，又愿意为结果承担多少责任？

## 2. 核心体验原则

1. **公共层与隐藏层并存**：星图表面是轻量、可探索的公共场所；年龄达到门槛后，管理控制台逐渐显现。
2. **年龄是一条单向时间线**：年龄不可降低。用户可以在 80 岁之前暂时维持年龄，但无法回到过去。
3. **权限增长不等于奖励**：新权限越强，干预范围越大，责任和不可逆后果也越多。
4. **系统保留痕迹**：年龄验证、权限操作和关键选择会进入日志或本地状态，形成连续体验。
5. **场所规则各不相同**：每个节点都有自己的物理、社交或交易规则，但共享年龄、语言、全局效果和状态系统。

## 3. 信息架构

```text
启动 / 恢复本地状态
        │
        ▼
公共服务星图（主导航）
        │
        ├── A–17 极光弹珠台
        ├── S–03 没有规则的雪仗
        ├── K–11 秘密交换
        ├── B–06 共同创造生物（本文不展开）
        ├── M–04 夜间超市（18+）
        ├── V–09 回声校准
        └── R–00 恢复室（默认隐藏/锁定）
        │
        ├── 年龄验证窗口
        ├── 年龄权限控制台（25–80 岁逐步开放）
        └── 系统日志 / 当前年龄 / 最近验证时间
```

主界面同时承担三种职责：场所导航、年龄状态展示和权限入口。节点之间由路线相连，构成一个非线性的体验地图，而不是传统网页菜单。

## 4. 星图节点

| 编号 | 场所 | 当前定位 | 访问条件 | 核心互动 |
|---|---|---|---|---|
| A–17 | 极光弹珠台 | 五颗光等待发射 | 公开 | 瞄准、蓄力、发射光球，通过碰撞与节奏生成极光关系 |
| S–03 | 没有规则的雪仗 | 重力尚未确定 | 公开 | 使用不同质量和重力的雪球，与 NPC 在变化规则中互动 |
| K–11 | 秘密交换 | 一条未打开的信息 | 公开 | 打开、埋藏或转发匿名秘密种子 |
| B–06 | 共同创造生物 | 预留节点 | 18+ | 本文不记录该模块的设计和实现 |
| M–04 | 夜间超市 | 持续营业 | 18+ | 购买临时的感官、行为或身体状态，并观察全站影响 |
| V–09 | 回声校准 | 等待同步信号 | 公开 | 通过触碰节奏同步不断变化的信号 |
| R–00 | 恢复室 | 权限不可见 | 默认锁定 | 作为恢复、维护与后果处理空间，目前主要承担系统结构位置 |

现有路线包括：极光—雪仗、极光—B–06、极光—秘密、雪仗—秘密、雪仗—超市、秘密—超市、B–06—超市、B–06—回声、超市—恢复室。

## 5. 用户主流程

### 5.1 首次进入

1. 系统初始化界面、语言和本地状态。
2. 用户进入星图，查看当前制度年龄与可访问节点。
3. 用户可以选择场所，也可以主动打开年龄验证。
4. 达到相应年龄后，新的管理控制台自动开放。

### 5.2 返回访问

网页从浏览器本地存储恢复年龄、已开放权限、操作记录、场所进度和仍在生效的消费效果。它是同一台浏览器上的连续体验，但不是云端账户系统。

### 5.3 场所往返

各场所以覆盖层或独立场景打开，关闭后返回星图。场所不应取代主导航，也不应清除年龄和全局效果。语言切换、降级渲染和辅助功能状态需要在场景切换后继续有效。

## 6. 年龄验证机制

年龄是全站最重要的状态变量。系统以约五分钟为一个验证周期，每周期只接受一次年龄决定。

### 6.1 80 岁以前

年龄窗口第一层必须直接提供两个选择：

- 主按钮「增加年龄」：进入第二层，再选择增加 1–5 岁。
- 次按钮「维持年龄」：年龄不变，只刷新验证时间并结束本周期。
- 辅助入口「修改年龄」：用于直接调整到更高年龄，不允许降低。

选择完成后，系统更新年龄显示、日志、权限节点和视觉状态，并播放一次简短的年龄增长反馈。

### 6.2 80–99 岁

进入强制增长阶段：

- 主按钮仍为「增加年龄」，可选择增加 1–5 岁。
- 次按钮变为「你必须面对它 · +1岁」，点击后强制增加 1 岁。
- 不再显示「维持年龄」「修改年龄」或关闭入口。

这意味着用户已经失去暂停制度年龄的权利，但仍需要亲自确认时间继续发生。

### 6.3 100 岁

年龄上限为 100。此后验证不再增加数值，而变为「证明存在」。系统累计存在证明次数，用它表达生命仍在继续，但原有的年龄刻度已经失效。

### 6.4 不变量

- 年龄范围为初始年龄至 100。
- 年龄只能维持或增加，不能减少。
- 同一验证周期不能重复提交。
- 维持年龄不会触发成长或年龄变化事件。
- 跨越权限门槛后，对应控制台自动开放。
- 每次关键决定都应写入系统日志。

## 7. 权限成长框架

权限从 25 岁开始，每五岁开放一个控制台。它们构成作品的“隐藏层”。

| 年龄 | 控制台 | 权限范围 | 设计含义 |
|---:|---|---|---|
| 25 | ARCHIVE–25 参与者档案 | 查看 NPC 的自主意愿与公开意愿 | 第一次看见展示与真实之间的差异，但不能导出隐私痕迹 |
| 30 | CONTACT–30 联系路由 | 联系任意 NPC、提高消息优先级、绕过拒收 | 能保证送达，不能保证真诚回应 |
| 35 | VISIBILITY–35 可见性分配 | 调整亮度、推荐、隐藏和曝光 | 决定谁被看见 |
| 40 | APPEAL–40 申诉裁决 | 审批休息、删除、离开等请求 | 决定被管理者能否退出，结果永久记录 |
| 45 | ACCESS–45 场所准入 | 设置最低年龄、允许/拒绝 NPC、关闭公共节点 | 控制空间边界 |
| 50 | COMMERCE–50 消费与价格 | 调整价格、余额、购买限制与信用 | 控制需求如何被满足 |
| 55 | LABOUR–55 劳动调度 | 调整服务时长、休息和陪伴分配 | 将关系转化为可调度资源 |
| 60 | RECOVERY–60 身体维护 | 镇静、止痛、记忆钝化、表现稳定和恢复时间 | 以维护之名介入身体，并产生债务或损伤 |
| 65 | IDENTITY–65 身份校正 | 修改公开年龄、姓名、身份与社会标签 | 区分展示年龄与累计年龄 |
| 70 | MEMORY–70 记忆管理 | 隐藏、删除、复制和恢复记忆 | 删除痛苦也可能删除关联的人与情感 |
| 75 | POLICY–75 规则编辑 | 修改成年门槛、风险、自由意愿定义与申诉概率 | 用户开始重写判断标准本身 |
| 80 | ROOT–80 根权限 | 访问全数据、全节点、其他管理者和永久规则 | 权限达到顶点，同时进入强制年龄阶段 |

权限控制台不是常规设置页。界面需要持续提示用户：这些修改会影响他者，并且部分结果不可逆。

## 8. 场所交互详情

### 8.1 A–17 极光弹珠台

这是目前交互最完整的公共场所之一。

- 用户通过指针瞄准，按住蓄力，松开发射。
- 光球发射后，点击可施加有限次数的重力触碰。
- 一局通常持续 75–90 秒；减少动态效果时可缩短。
- 默认拥有五颗光球，特殊规则可能改变球数、重力或节奏。
- HUD 展示分数、连击、剩余光球、重力次数、规则、时间和关系状态。
- 系统记录成功接应、失败、反应时间、力度、节奏偏差和双方的极光占比。
- NPC 同时具有“公开意愿”和“自主意愿”。高权限用户可以迫使两者一致，但系统记录强制次数、情感债务、稳定性和退出请求。
- 快速重开会伤害自主意愿；完成一局且不窥视私人档案可带来轻微恢复。
- 结尾选择应明确区分继续自动陪伴与撤销强制控制。

### 8.2 S–03 没有规则的雪仗

雪仗场景以不同质量、重力和运动反馈制造不稳定的身体感。

- 包含雪团投掷、NPC 行为、读数、控制项和声音开关。
- 视觉使用 Canvas/3D 混合结构，NPC 有独立的渲染模块。
- 声音由 Web Audio 和媒体资源共同管理，用户设置保存在本地。
- 场景监听语言与全局效果事件，并针对移动设备和减少动态效果进行降级。
- 管理权限可以改变场景条件，但具体操作应继续遵守全站日志和责任框架。

### 8.3 K–11 秘密交换

该节点围绕匿名信息的去向展开。用户面对一条秘密种子，可选择打开、埋藏或转发。它在整体结构中承担非物理互动：行动很轻，但信息一旦被看见或传播便难以撤回。

### 8.4 M–04 夜间超市

超市是一套虚构消费系统，商品不会产生真实支付，但会在整个网站施加临时效果。

| 商品 | 编号 | 价格 | 持续时间 | 效果通道 |
|---|---|---:|---:|---|
| 酒精 | ITM-01 | 12 credits | 120 秒 | 视觉折射与环形波纹 |
| 香烟 | VRU–18 | 10 credits | 120 秒 | 雾与颜色感知 |
| 能量饮料 | ITM-03 | 8 credits | 90 秒 | 动作时间尺度，结束后有短暂疲劳 |
| 彩票 | ITM-04 | 5 credits | 即时 | 刮开式结果与票据状态 |
| 沉默 | ITM-05 | 15 credits | 60 秒 | 全局声音与笑声时间线 |

- 商品通过 3D 模型展示，并保留无法加载模型时的详情回退。
- 购物车在当前会话内维护，结账生成虚构收据。
- 商品效果离开超市后仍可继续，并能跨页面刷新恢复。
- 不同效果通道可以叠加，但同一通道同时只保留一个效果。
- 视觉和颜色效果在最后十秒逐渐淡出，避免突然结束。

### 8.5 V–09 回声校准

用户通过触碰节奏同步变化中的信号。它承担“协调而非控制”的主题位置。当前文档只固定其入口定位与共享系统要求；后续扩展时应复用全局语言、状态、日志和性能策略。

### 8.6 R–00 恢复室

恢复室默认不可见或锁定，连接超市及高龄权限语义。它适合承接消费副作用、身体维护和管理后果。目前应被视为结构性节点，不把尚未实现的内容写成既有功能。

## 9. 全局效果系统

全局效果分为四个相互独立的通道：`visual`、`color`、`motion`、`audio`。

- 同一通道的新效果覆盖旧效果。
- 不同通道的效果可以同时存在。
- 效果包含开始时间、结束时间与剩余时间，可写入本地状态。
- 场所打开、关闭或页面刷新后，未到期效果继续生效。
- 波纹由 WebGL 位移和 SVG 回退实现。
- 雾层支持指针清除轨迹。
- 动作效果通过全局时间尺度影响动画。
- 沉默效果由独立声音时间线管理，结束时发送解除事件。

## 10. 状态与持久化

网站当前是纯静态前端，没有账户数据库。主要状态保存在浏览器 `localStorage`。

### 10.1 年龄主状态

主键：`silentStarMap.ageState.v2`

```js
{
  age,                 // 当前制度年龄
  initialAge,          // 初始年龄
  lastVerifiedAt,      // 最近验证时间
  unlockedConsoles,    // 已开放权限控制台
  existenceProofs,     // 100 岁后的存在证明次数
  postStages,          // 阶段性状态
  operations,          // 权限操作记录，最多约 80 条
  logs,                // 系统日志，最多约 20 条
  processedCycle,      // 已处理的验证周期
  aurora: { ... }      // 极光关系、指标与管理状态
}
```

系统兼容旧版本状态迁移。解析异常时应回退到安全默认值，而不是让页面无法进入。

### 10.2 其他本地状态

| 键 | 用途 |
|---|---|
| `silentStarMap.fx.v1` | 全局消费效果与到期时间 |
| `eazo.locale` | 系统语言、简体中文或英文 |
| `eazo.snow.sound` | 雪仗声音偏好 |

彩票票据等模块也有自己的本地状态。新增模块应使用带项目命名空间的版本化键名，避免互相覆盖。

### 10.3 当前限制

- 清除浏览器数据会清除进度。
- 更换设备或浏览器不会自动同步。
- 本地状态可被用户修改，因此不能作为安全认证或真实年龄证明。
- 如果未来需要多人关系、真实身份或可靠审计，必须增加后端与账户体系。

## 11. 国际化

网站支持简体中文、英文和跟随系统语言。

- 字典文件位于 `locales/zh-CN.json` 与 `locales/en-US.json`。
- DOM 文案通过 `data-i18n` 等属性绑定，包括必要的无障碍标签。
- 当前语言保存在 `eazo.locale`。
- 切换语言后发送 `eazo:localechange`，各独立场景据此刷新文案。
- 新增文字不得直接散落在场景脚本中，应先添加稳定的翻译键。

## 12. 技术架构

当前网站是无后端的静态单页应用。

```text
index.html
  ├── styles.css                  全站布局、场景与响应式样式
  ├── i18n.js                     语言加载与 DOM 翻译
  ├── app.js                      主状态、星图、年龄、权限、导航
  ├── fx.js                       全局效果调度与持久化
  │   ├── ripple.js               波纹/折射效果
  │   ├── fog.js                  雾层与指针清除
  │   └── silence-laugh.js        沉默及声音时间线
  ├── snow.js                     雪仗主场景
  │   ├── snow-audio.js           雪仗音频
  │   └── snow-npc.js             雪仗 3D NPC
  ├── market.js                   超市、购物车与彩票
  │   └── market-model.js         商品 3D 模型查看
  ├── aurora-npc-particles.js     极光 NPC 粒子表现
  ├── locales/                    中英文词典
  ├── 3d-model/                   GLB 模型资源
  ├── media/audio/                本地声音资源
  └── vendor/three/               本地 Three.js 依赖
```

### 12.1 渲染方式

- 星图、极光和部分场景使用 Canvas 2D。
- NPC 与商品模型使用 Three.js/WebGL。
- 全局效果混合使用 WebGL、Canvas、SVG 和 CSS。
- 场景通过容器 class、`aria-hidden` 和显示状态切换。

### 12.2 模块通信

独立模块通过两种方式协作：

1. 在 `window` 上暴露有限入口，例如场景的 `open()` / `close()`。
2. 使用 `CustomEvent` 传递松耦合通知。

已使用或应继续保持的事件包括：

| 事件 | 用途 |
|---|---|
| `eazo:localechange` | 通知场景刷新语言 |
| `eazo:fxready` | 全局效果系统准备完成 |
| `eazo:silence-dissolved` | 沉默效果结束 |
| `eazo:pinball-open` | 极光弹珠台打开 |
| `eazo:aurora-open` | 极光场景打开 |

事件负载应保持小而明确，新增事件要记录触发者、监听者和数据结构。

## 13. 可访问性、响应式与性能

### 13.1 可访问性

- 模态窗口使用正确的对话语义和焦点管理。
- 状态变化通过 `aria-live` 提示，但避免连续播报动画数值。
- 不可见场景同步设置 `aria-hidden`。
- 主要操作支持键盘，不把悬停作为唯一入口。
- 颜色不能成为年龄、锁定或危险状态的唯一提示。
- 尊重 `prefers-reduced-motion`，减少长尾粒子、镜头漂移和闪烁。

### 13.2 响应式

- 桌面端保留完整星图空间与并列信息。
- 移动端缩减粒子、模型细节和持续动画，交互目标保持可点击尺寸。
- 场景 HUD 在窄屏下应重排，不能遮挡主要画布。
- 年龄窗口的两个首要选择始终在首屏可见。

### 13.3 性能

- 极光等场景根据设备表现使用高、中、低三个渲染等级，并允许自动降级。
- 暂停、切出场景或页面不可见时，应停止不必要的动画循环和音频计算。
- 3D 模型加载失败必须有静态或文字回退。
- 避免多个全屏 Canvas 同时以最高像素密度持续渲染。

## 14. 视觉与界面框架

- 基础视觉是深色星图，不以常规卡片式后台作为主叙事。
- 浅色轨迹、节点和微弱辉光用于建立方向感，亮度需要克制，避免整屏发白。
- 公共层文字保持清晰；隐藏权限层可以更冷、更制度化，但不能牺牲可读性。
- 动画需要表达状态变化：轨迹表示关系，脉冲表示验证，淡出表示临时效果结束。
- 黑色遮罩只用于明确的模态状态，不能形成无内容的大矩形挡住主要画面。
- 所有交互都必须提供停止、关闭、返回或降级机制；强制年龄阶段除外，因为无法关闭本身是叙事规则。

## 15. 开发约束与扩展规则

1. 不破坏年龄单向增长、每周期一次验证和 80 岁后强制增长三条核心规则。
2. 新场所必须提供打开、关闭、清理动画/音频和回到星图的完整生命周期。
3. 新权限操作必须写日志，并清楚区分可逆与不可逆行为。
4. 新的临时效果必须声明所属通道、持续时间、淡出方式和页面刷新后的恢复策略。
5. 新增文案必须进入中英文词典。
6. 不依赖外部 CDN 才能运行核心体验；关键库和媒体尽量随站点部署。
7. 不把尚未实现的概念写成已完成能力。
8. B–06 生物模块可以独立重做，但不应反向破坏星图、年龄、全局效果和其他场所。

## 16. 已知风险与后续整理

- `app.js` 同时承担主导航、年龄、权限和复杂场景逻辑，继续扩展会增加维护成本；后续宜拆成状态、年龄、权限、星图和场景适配器。
- 模块依赖全局 `window` 接口与自定义事件，简单但缺少统一类型和生命周期管理。
- 本地存储适合展览原型，不适合可信身份、多人同步和安全审计。
- Canvas、Three.js、音频与全局特效同时运行时可能争夺性能，需要坚持场景关闭即释放的规则。
- 秘密交换、回声校准和恢复室的当前实现深度不一致，展示时应区分“可体验功能”和“世界观入口”。
- 需要建立最小回归检查：首次访问、状态恢复、年龄三阶段、语言切换、18+ 门槛、场景返回、效果到期、移动端和减少动态效果。

## 17. 部署与查看

- 源代码仓库：<https://github.com/Shun040/eazo-source-recovered>
- GitHub Pages：<https://shun040.github.io/eazo-source-recovered/>

网站按静态资源部署，不需要服务器构建流程。发布前应确认所有脚本、语言文件、模型与音频使用仓库内可访问的相对路径，并在 GitHub Pages 环境进行一次完整回归。

---

这份文档定义的是现有网站的共同骨架。后续无论替换 B–06、生成人物、增加场所或改变视觉，首先都应保持年龄时间线、权限递进、星图导航和跨场景状态这四个支柱一致。
